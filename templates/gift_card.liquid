{% layout none %}
<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <title>Gift Card</title>
  </head>
  <body>
    <h1>Gift Card</h1>
    <p>Value: {{ gift_card.initial_value | money }}</p>
  </body>
</html>
